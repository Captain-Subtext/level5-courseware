# cursor-learning/client/.env

# Supabase (used by client for auth UI and potentially direct reads if needed)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# API Server URL (points to our Express server)
VITE_API_BASE_URL=  # For local development
# VITE_API_BASE_URL=  # For local iphone development

# Stripe (Publishable key for client-side Stripe.js)
VITE_STRIPE_PUBLISHABLE_KEY=

